# Dependencias directas de IPTVViewer
# Generado: 2026-07-04

# UI
PyQt6>=6.5

# Reproductores
python-vlc>=3.0
mpv>=1.0

# HTTP / descargas
requests>=2.28

# Proxy Tor
torpy>=1.0
# PySocks: requerido por requests/urllib3 para consumir proxies SOCKS5
# (el proxy SOCKS5 de Tor se usa via HTTP_PROXY=socks5h://...) - BUG-01
PySocks>=1.7.1

# Tests
pytest>=8.0
pytest-qt>=4.2

# Dev tools (lint + type checking)
ruff>=0.16
mypy>=2.0
